#include <iostream>
#include <string>
#include <sstream>
#include <vector>
#include <tuple>
#include <cassert>
 
int main() \{
    std::cin.sync_with_stdio(false);

{ parser }

{ checker }
 
    return 0;
}