package main

import (
    "bufio"
    "os"
    "strconv"
	"math"
)

func main() \{
	input := bufio.NewScanner(os.Stdin)
	buf := make([]byte, 10 * 1024)
	input.Buffer(buf, math.MaxInt64)
    input.Split(bufio.ScanWords)

{ parser }

{ checker }

}